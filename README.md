# 🚂 RZD Infrastructure Management System

> Современная система учета и управления объектами инфраструктуры, пересекающимися с железнодорожными путями

[![Python](https://img.shields.io/badge/Python-3.13+-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.118+-green.svg)](https://fastapi.tiangolo.com/)
[![SQLAlchemy](https://img.shields.io/badge/SQLAlchemy-2.0+-red.svg)](https://www.sqlalchemy.org/)
[![License](https://img.shields.io/badge/License-Proprietary-yellow.svg)](LICENSE)

## 📋 Описание

Система предназначена для централизованного учета и управления информацией о различных объектах инфраструктуры, которые пересекаются с железнодорожными путями. Приложение обеспечивает полный цикл работы с объектами: создание, поиск, фильтрацию и управление данными.

### 🎯 Основные возможности

- ✅ **Универсальная модель объектов** - единая таблица для всех типов инфраструктуры
- 🔍 **Мощный поиск** - полнотекстовый поиск с фильтрацией по множеству параметров
- 🔐 **Система аутентификации** - JWT-токены и управление пользователями
- 📊 **Гибкая архитектура** - трехслойная архитектура для масштабируемости
- 🗺️ **Геопозиционирование** - координаты объектов в формате WGS84
- 📝 **Специфические данные** - JSON-хранилище для уникальных характеристик каждого типа объекта

## 🏗️ Архитектура

Проект построен на основе **трехслойной архитектуры**:

```
┌─────────────────────────────────────┐
│   Presentation Layer (Routers)      │  ← HTTP запросы/ответы
├─────────────────────────────────────┤
│   Business Logic Layer (Services)   │  ← Бизнес-логика
├─────────────────────────────────────┤
│   Data Access Layer (Repositories)  │  ← Работа с БД
└─────────────────────────────────────┘
```

### Слои архитектуры

- **Presentation Layer** (`app/routers/`) - обработка HTTP-запросов, валидация, маршрутизация
- **Business Logic Layer** (`app/services/`) - бизнес-логика, валидация правил, маппинг данных
- **Data Access Layer** (`app/repositories/`) - работа с базой данных, CRUD-операции

## 🛠️ Технологический стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Python** | 3.13+ | Язык программирования |
| **FastAPI** | 0.118+ | Современный веб-фреймворк |
| **SQLAlchemy** | 2.0+ | ORM для работы с БД |
| **SQLite** | - | Реляционная база данных |
| **Pydantic** | - | Валидация данных и схемы |
| **PyJWT** | 2.10+ | JWT-аутентификация |
| **Uvicorn** | 0.37+ | ASGI-сервер |
| **Poetry** | - | Управление зависимостями |

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.13 или выше
- Poetry (для управления зависимостями)

### Установка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd ржд
```

2. **Установите зависимости**
```bash
poetry install
```

3. **Настройте переменные окружения**

Создайте файл `.env` в корне проекта:
```env
TOKEN_SECRET=your-secret-key-here
DB_URL=sqlite:///./app.db
```

4. **Запустите приложение**
```bash
poetry run dev
```



5. **Откройте в браузере**
- Swagger UI: http://localhost:8080/docs

## 📚 API Документация

### 🔐 Аутентификация

#### Получить токен доступа
```http
POST /users/get_token
Content-Type: application/json

{
  "snils": "123-456-789 00",
  "password": "your-password"
}
```

#### Регистрация пользователя
```http
POST /users
Content-Type: application/json

{
  "snils": "123-456-789 00",
  "password": "your-password"
}
```

### 🏗️ Объекты инфраструктуры

#### Мосты

**Получить список мостов**
```http
GET /bridges/
```

**Создать мост** (требуется аутентификация)
```http
POST /bridges/
Authorization: Bearer <token>
Content-Type: application/json

{
  "name": "Мост через Волгу",
  "owner": "РЖД Поволжская",
  "year_commissioned": 1985,
  "bridge_type": "rail",
  "length_m": 250,
  "centroid": {
    "lat": 53.388960,
    "lon": 50.319780
  }
}
```

#### Насыпи

**Получить список насыпей**
```http
GET /embankments/
```

**Создать насыпь** (требуется аутентификация)
```http
POST /embankments/
Authorization: Bearer <token>
Content-Type: application/json

{
  "name": "Насыпь на участке Самара-Тольятти",
  "owner": "РЖД Поволжская",
  "year_commissioned": 1960,
  "type": "rail",
  "centroid": {
    "lat": 53.392693,
    "lon": 50.311848
  }
}
```

#### ЛЭП (Линии электропередачи)

**Получить список ЛЭП**
```http
GET /powerlines/
```

**Создать ЛЭП** (требуется аутентификация)
```http
POST /powerlines/
Authorization: Bearer <token>
Content-Type: application/json

{
  "name": "ВЛ 500кВ Самара-Тольятти",
  "owner": "СамараЭнерго",
  "year_commissioned": 1980,
  "voltage_kv": 500,
  "centroid": {
    "lat": 53.228661,
    "lon": 50.285445
  }
}
```

#### Трубопроводы

**Получить список трубопроводов**
```http
GET /pipelines/
```

**Создать трубопровод** (требуется аутентификация)
```http
POST /pipelines/
Authorization: Bearer <token>
Content-Type: application/json

{
  "name": "Магистральный газопровод",
  "owner": "Газпром Трансгаз Самара",
  "year_commissioned": 1985,
  "medium": "gas",
  "diameter_mm": 720,
  "centroid": {
    "lat": 53.405487,
    "lon": 50.288284
  }
}
```

#### Элементы

**Получить список элементов**
```http
GET /items/
```

**Создать элемент**
```http
POST /items/
Content-Type: application/json

{
  "name": "Элемент инфраструктуры",
  "description": "Описание элемента"
}
```

**Получить элемент по ID**
```http
GET /items/{id}
```

**Удалить элемент**
```http
DELETE /items/{id}
```

### 🔍 Поиск объектов

**Поиск с фильтрами**
```http
POST /search/objects
Content-Type: application/json

{
  "query": "мост",
  "object_types": ["bridge"],
  "owners": ["РЖД Поволжская"],
  "year_from": 1960,
  "year_to": 2020,
  "limit": 20,
  "offset": 0
}
```

**Параметры поиска:**
- `query` - текст для полнотекстового поиска (поиск в названии, владельце, описании)
- `object_types` - фильтр по типам: `bridge`, `embankment`, `pipeline`, `powerline`, `item`
- `owners` - фильтр по владельцам
- `year_from` / `year_to` - диапазон годов ввода в эксплуатацию
- `limit` - количество результатов (1-100, по умолчанию 20)
- `offset` - смещение для пагинации

### 🚨 Аварии

**Получить список аварий**
```http
GET /accident/
```

**Создать метку аварии** (требуется аутентификация)
```http
POST /accident/
Authorization: Bearer <token>
Content-Type: application/json

{
  "responsible": "ООО Подрядчик",
  "date": "2024-01-15",
  "accident_type": "rail",
  "centroid": {
    "lat": 53.400000,
    "lon": 50.300000
  }
}
```

### 🏥 Системные эндпоинты

**Проверка состояния**
```http
GET /health
```

## 📁 Структура проекта

```
app/
├── core/                    # Конфигурация приложения
│   └── config.py           # Настройки
├── db/                      # Работа с базой данных
│   └── session.py          # Сессии SQLAlchemy
├── models/                  # SQLAlchemy модели
│   ├── object.py           # Универсальная модель объектов
│   ├── user.py             # Модель пользователя
│   └── accidents.py        # Модель аварий
├── repositories/            # Data Access Layer
│   ├── base_repository.py  # Базовый репозиторий
│   ├── object_repository.py
│   ├── user_repository.py
│   ├── accident_repository.py
│   └── search_repository.py # Репозиторий поиска
├── routers/                 # Presentation Layer
│   ├── bridges.py          # Роутер мостов
│   ├── embankments.py      # Роутер насыпей
│   ├── pipelines.py        # Роутер трубопроводов
│   ├── powerlines.py       # Роутер ЛЭП
│   ├── items.py            # Роутер элементов
│   ├── search.py           # Роутер поиска
│   ├── users.py            # Роутер пользователей
│   └── accidents.py         # Роутер аварий
├── schemas/                 # Pydantic схемы
│   ├── bridge.py
│   ├── embankment.py
│   ├── pipeline.py
│   ├── powerline.py
│   ├── item.py
│   ├── search.py
│   ├── user.py
│   ├── accidents.py
│   └── geo.py              # Географические схемы
├── services/                # Business Logic Layer
│   ├── object_service.py   # Сервис объектов
│   ├── user_service.py     # Сервис пользователей
│   ├── accident_service.py # Сервис аварий
│   ├── search_service.py   # Сервис поиска
│   ├── mappers.py          # Мапперы для преобразования данных
│   ├── seed.py             # Инициализация тестовых данных
│   └── snils.py            # Валидация СНИЛС
├── auth.py                  # Аутентификация и авторизация
├── permissions.py           # Система прав доступа
├── settings.py              # Настройки приложения
├── main.py                  # Главный файл приложения
└── run.py                   # Точка входа
```

## 🗄️ Модель данных

### Универсальная модель Object

Все объекты инфраструктуры хранятся в единой таблице `objects` с общими полями и специфическими данными в JSON:

**Общие поля:**
- `id` - уникальный идентификатор
- `name` - название объекта
- `owner` - владелец объекта
- `type` - тип объекта (enum: `bridge`, `embankment`, `pipeline`, `powerline`, `item`)
- `description` - описание (опционально)
- `year_commissioned` - год ввода в эксплуатацию
- `centroid_lat` / `centroid_lon` - координаты точки пересечения
- `specific_data` - специфические данные в формате JSON

**Специфические данные по типам:**

| Тип | Поля в `specific_data` |
|-----|------------------------|
| `bridge` | `bridge_type`, `length_m` |
| `embankment` | `type` |
| `pipeline` | `medium`, `diameter_mm` |
| `powerline` | `voltage_kv` |
| `item` | - |

### Примеры данных

**Мост:**
```json
{
  "id": 1,
  "name": "Мост через Волгу",
  "owner": "РЖД Поволжская",
  "type": "bridge",
  "year_commissioned": 1985,
  "centroid_lat": 53.388960,
  "centroid_lon": 50.319780,
  "specific_data": {
    "bridge_type": "rail",
    "length_m": 250
  }
}
```

**ЛЭП:**
```json
{
  "id": 2,
  "name": "ВЛ 500кВ",
  "owner": "СамараЭнерго",
  "type": "powerline",
  "year_commissioned": 1980,
  "centroid_lat": 53.228661,
  "centroid_lon": 50.285445,
  "specific_data": {
    "voltage_kv": 500
  }
}
```

## 🔍 Примеры использования

### Поиск всех мостов определенного владельца

```python
import requests

response = requests.post(
    "http://localhost:8000/search/objects",
    json={
        "object_types": ["bridge"],
        "owners": ["РЖД Поволжская"],
        "limit": 50
    }
)

bridges = response.json()["results"]
```

### Создание нового объекта с аутентификацией

```python
import requests

# Получаем токен
token_response = requests.post(
    "http://localhost:8000/users/get_token",
    json={
        "snils": "123-456-789 00",
        "password": "your-password"
    }
)
token = token_response.json()

# Создаем мост
bridge_response = requests.post(
    "http://localhost:8000/bridges/",
    headers={"Authorization": f"Bearer {token}"},
    json={
        "name": "Новый мост",
        "owner": "РЖД",
        "year_commissioned": 2024,
        "bridge_type": "rail",
        "length_m": 300,
        "centroid": {"lat": 53.4, "lon": 50.3}
    }
)
```

### Поиск объектов по году ввода в эксплуатацию

```python
import requests

response = requests.post(
    "http://localhost:8000/search/objects",
    json={
        "year_from": 1980,
        "year_to": 2000,
        "limit": 100
    }
)

old_objects = response.json()["results"]
```

## 🧪 Инициализация данных

При первом запуске приложение автоматически создает тестовые данные с реальными координатами объектов в Самарской области:

- **4 моста** - различные типы и размеры
- **6 насыпей** - железнодорожные и автомобильные
- **11 ЛЭП** - различное напряжение (35-500 кВ)
- **4 трубопровода** - нефть и газ, разные диаметры

Все координаты соответствуют реальным точкам пересечения объектов с ЖД путями.

## 🔧 Разработка

### Добавление нового типа объекта

1. **Добавьте новый тип в enum** (`app/models/object.py`):
```python
class ObjectType(str, Enum):
    # ... существующие типы
    NEW_TYPE = "new_type"
```

2. **Создайте схемы** (`app/schemas/new_type.py`):
```python
class NewTypeCreate(BaseModel):
    name: str
    owner: str
    # ... специфические поля
```

3. **Добавьте методы в сервис** (`app/services/object_service.py`):
```python
def get_new_types(self) -> list[dict[str, Any]]:
    # ...

def create_new_type(self, data: NewTypeCreate) -> dict[str, Any]:
    # ...
```

4. **Создайте роутер** (`app/routers/new_types.py`):
```python
@router.get("/")
def list_new_types(db: Session = Depends(get_db)):
    # ...
```

5. **Подключите роутер** в `app/main.py`

### Формат координат

Координаты хранятся в формате **WGS84** (EPSG:4326):
- **Широта (lat)**: от -90 до 90 градусов
- **Долгота (lon)**: от -180 до 180 градусов

Для Самарской области:
- Широта: ~51.0 - 55.5
- Долгота: ~47.0 - 52.0

## 🧪 Тестирование

### Запуск приложения в режиме разработки

```bash
poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Флаг `--reload` включает автоматическую перезагрузку при изменении кода.

### Проверка работоспособности

```bash
# Проверка здоровья приложения
curl http://localhost:8000/health

# Получение списка мостов
curl http://localhost:8000/bridges/
```

## 📝 Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Секретный ключ для JWT токенов
TOKEN_SECRET=your-super-secret-key-change-in-production

# URL базы данных
DB_URL=sqlite:///./app.db
```

## 🚀 Деплой

### Production настройки

1. Измените `TOKEN_SECRET` на надежный случайный ключ
2. Настройте CORS для вашего домена в `app/main.py`
3. Используйте PostgreSQL вместо SQLite для production
4. Настройте reverse proxy (nginx) для статики
5. Используйте процесс-менеджер (systemd, supervisor)

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Проект создан для внутреннего использования РЖД. Все права защищены.

## 👥 Авторы

- **Команда разработки РЖД** - *Начальная разработка*

## 🙏 Благодарности

- FastAPI за отличный фреймворк
- SQLAlchemy за мощный ORM
- Всем контрибьюторам проекта

---

**Сделано с ❤️ для РЖД**
